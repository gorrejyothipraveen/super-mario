{
  "name": "super-mario",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,frontend -c cyan,yellow \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
